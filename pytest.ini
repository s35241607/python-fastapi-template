[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --ignore=scripts/ --asyncio-mode=auto
markers =
    unit: Unit tests
    integration: Integration tests
